#include "FcGL_Button.h"

FcGL_Button_t FcGL_Button(FcGL_Button_options_t *options, FcImage_t *image, FcGLWindow_t *window) {
    GLuint i, j, width, height;
    FcGL_Button_t button;

    //width = options->width;
    //height = options->height;

    //for (i = options->y - height / 2; i < image->height && i < height; i++) {
    //    for (j = options->x - width / 2; j < image->width && j < width; j++) {

    //    }
    //}
    return button;
}

